# Reports template

This is just a LaTeX file with a collection of commonly used packages, useful options already set,
and some example code. 

## Usage

Copy `main.tex`. This is your main project file, edit as needed. Overleaf and most modern LaTeX installs
support all the used packages out of the box.