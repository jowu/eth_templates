% See documentation for class options.
\documentclass[11pt,mscthesis,report]{eapsthesis}

% Only for the sample text below. DELETE THESE WHEN YOU START YOUR PROJECT.
\usepackage{lipsum}
\usepackage{amssymb}
\usepackage{amsmath}
% DELETE THE PACKAGES ABOVE BEFORE YOU START YOUR PROJECT.

% Recommended to set line spacing to 1.5 as required.
\usepackage{setspace}

% Default formatting packages. Just load these, you'll need them anyway.
\usepackage{booktabs}   % for nice tables
\usepackage{subcaption} % for combining multiple figures
\usepackage{caption}    % To define custom figure caption formatting.
\captionsetup{font=small,labelfont={bf},width=0.95\textwidth} % smaller caption font size

% Extra formatting packages, load as needed.
% Defines some extra symbols like \degree
% \usepackage{gensymb}
% Defines commands to merge cells in tables.
% \usepackage{multirow}
% \usepacakge{multicolumn}
% \usepackage{pdflscape}  % to insert landscape pages, e.g. for large tables
% \usepackage{afterpage}  % to clean up floats and similar. Ignore this if you don't know what it does.

% Hyperref is enabled by default, and uses default options. Use the class option 'nohyper' to turn off hyperref.
% To change formatting behavior, use \hypersetup with appropriate options.
% This example removes all visible marking from hyperlinks, so it looks clean for a print layout.
% \hypersetup{colorlinks=false, pdfborder={0 0 0}}

% Math fonts and extra environments for formulas.
% \usepackage{amsmath}
% \usepackage{mathtools}
% \usepackage{textcomp}

% Internal references, this MUST be the last package loaded, except commands below.
\usepackage{cleveref}
% Custom commands the improve editing workflows and define some helpers.
% See documentation for use, remove if in doubt.
% \usepackage[edit]{commands}


% Citation formats and processors. The class file loads biblatex with apa style.
\addbibresource{bib.bib}


\title{My Thesis - A very long title\\ which runs over two
lines} %compulsory
\date{\today}
\author{Random \textsc{Student}}
\studentid{xx-yyy-zzzz}
\specialization{Master in Earth Sciences, Major Geology}

\mainadvisor{Prof.\ Dr.\ Some \textsc{Professor}}{Institute for Geology}
\coadvisor{Dr.\ A \textsc{Researcher}}{Institute for Geochemistry and Petrology}

\begin{document}

\maketitle %generates the titlepage, this is FIXED. Parameters are above.

\onehalfspace % sets linespacing to 1.5, as required for the BSc thesis.
\frontmatter %generates the frontmatter settings, this is FIXED

\begin{abstract}
This is a very abstract abstract. 

\lipsum
\end{abstract}

\begin{abstract}[Zusammenfassung]
optional, use only if your external advisor requires it in his/her
language 
\\

\lipsum
\end{abstract}

% Can be places here, or at the end in the end of the document.
\begin{acknowledgements}
\lipsum 
\end{acknowledgements}

\tableofcontents 

% \listoffigures %optional
% \listoftables %optional

\mainmatter

\section{Introduction}

\lipsum



\section[Short title]{A chapter title which will run over two lines --- it's for
  testing purpose}

\lipsum[1-2]

\section{The first section}
\lipsum[3-4]

 \section{The second, math section}

\textbf{Theorem 1 (Residue Theorem).}
Let $f$ be analytic in the region $G$ except for the isolated singularities $a_1,a_2,\ldots,a_m$. If $\gamma$ is a closed rectifiable curve in $G$ which does not pass through any of the points $a_k$ and if $\gamma\approx 0$ in $G$ then
\[
\frac{1}{2\pi i}\int_\gamma f = \sum_{k=1}^m n(\gamma;a_k) \text{Res}(f;a_k).
\]
\textbf{Theorem 2 (Maximum Modulus).}
\emph{Let $G$ be a bounded open set in $\mathbb{C}$ and suppose that $f$ is a continuous function on $G^-$ which is analytic in $G$. Then}
\[
\max\{|f(z)|:z\in G^-\}=\max \{|f(z)|:z\in \partial G \}.
\]

\section[third]{A very very long section, titled ``The third section'', with
  a rather  short text alternative (third)}
\lipsum \texttt{Some Test}

\nocite{*}

\appendix %optional, use only if you have an appendix

% In report style theses, having more than one appendix section gets confusing. But can be done.
% Section numbering changes from 1,2,3, to A, B, C.
\section{Appendix}
\lipsum 

\backmatter

\section{Glossary} %optional

\printbibliography


%\theindex %optional, use only if you have an index, must use
	  %\makeindex in the preamble
% \lipsum

\end{document}
